{# Pobieramy ścieżkę do produktu z nagłówka TEGO MODUŁU #}
{% set featured_product_path = module.header.featured_product_main|keys|first %}

{% if featured_product_path %}
    {% set featured_product = page.find(featured_product_path) %}
    {% set product_list = page.find('/produkty').children.order('title', 'asc') %}
    {% set main_image = (featured_product.media.images(featured_product.header.small_image)|first) %}

    <section class="max-w-7xl mx-auto p-0 sm:p-4 md:p-6 lg:p-8">
        {# ... Cały kod HTML z lewą i prawą kolumną ... #}
        {# Nie wklejam go ponownie dla zwięzłości, ale ma tu być cała ta sekcja #}
    </section>
{% endif %}